---
id: T009
title: モードC「さいごまで　うとう」(1局とおしプレイ、弱CPU・パス・終局・石数勝負)
status: done
assignee: codex
attempts: 0
---

# T009: モードC「さいごまで　うとう」(1局とおしプレイ、弱CPU・パス・終局・石数勝負)

## 目的

設計書「モード C: さいごまで　うとう」を実装する。弱い CPU 相手に1局を最後まで打ち切り、パス・終局・石数勝負を体験できるようにする(実際のボードで1局完走するための総仕上げ)。

## 背景・コンテキスト

- 設計書 `othello-beginner-design.md` の「モード C」節が仕様の正。§1(幼児向け方針)・§2(自分の手番のフローとメッセージ文言)も参照。
- コアロジック `app/src/core/othello.ts` に `legalMoves` / `flippedDiscs` / `applyMove` / `hasAnyMove` / `isGameOver` / `countDiscs` が揃っている。
- 練習フローの状態機械は `app/src/practice/practiceMachine.ts`(placing→flipping→result)。**自分の手番の挙動(置く→手動ひっくり返し→OK、ミス3種、メッセージ)はモードA と同一にすること**。共通化の方法(状態機械の再利用 or 対局用状態機械から practiceMachine のロジック関数を呼ぶ)は実装者裁量だが、文言・カウント挙動の重複定義はしない。
- T010(がんばりの記録)が対局結果を使う予定。終局時に「完走したこと」「勝敗」を呼び出し側が受け取れる形にしておく(コールバック or 戻り値。localStorage への保存自体は T010)。
- T008(キャレット修正)が先行して main に入っている想定。最新 main から作業すること。

## 変更対象

- `app/src/game/gameMachine.ts` — 対局の状態機械(純粋ロジック、新規)
- `app/src/game/gameMachine.test.ts` — ユニットテスト(新規)
- `app/src/game/cpu.ts` — CPU 着手選択(合法手から一様ランダム。乱数注入可能にしてテスト容易に)(新規)
- `app/src/game/GameScreen.tsx`(+CSS) — 対局画面(新規)
- `app/src/home/HomeScreen.tsx` — 「さいごまで　うとう」ボタン追加
- `app/src/app.tsx` — 画面切替に対局を追加

## 要件

### 対局の状態機械(UI から独立した純粋ロジック)

1. プレイヤーはくろ(先手)固定、CPU はしろ。開始局面から。
2. **自分の手番**: モードA と同一の placing→flipping フロー(非合法タップ「そこには　おけないよ！」+おくミス、間違い石「そのこまは　ひっくりかえせないよ！」+ひっくりかえしミス、漏れOK「まだ　ひっくりかえる　こまが　あるよ」+はやおしミス)。OK で確定したら CPU 手番へ。ミス3種は**1局を通して累積**。
3. **CPU の手番**: 「あいての　ばん」を表示。CPU は合法手から一様ランダムに選び、置いた石と裏返しは**自動で適用**(裏返しアニメーションは Board の既存機能で発火する)。CPU の思考待ちとして 600〜1000ms 程度の遅延を入れる(即打ちだと何が起きたか分からないため)。
4. **パス**: 自分に合法手がない → 「おける　ばしょが　ないよ！　ぱす！」を表示し、OK ボタンで CPU 手番へ。CPU に合法手がない → 「あいては　ぱす！」を表示して自分の手番へ(自動で進んでよいが、メッセージは読める時間を確保)。
5. **終局**(`isGameOver`): 石数を数えて「くろ　◯こ　しろ　◯こ」を表示し、勝敗メッセージ: プレイヤー勝ち「あなたの　かち！」(だいせいこう相当の花丸演出)、負け「あなたの　まけ」、引き分け「ひきわけ」。ミス3種の内訳も表示する。
6. 終局時に結果(勝敗・石数・ミス合計・完走フラグ)を呼び出し側に通知する仕組みを用意(T010 が購読する。このタスクでは console 出力や callback プロップまでで、保存はしない)。
7. 「もういちど」ボタンで新しい1局を開始できる。「おわる」でホームへ(途中終了は結果通知しない)。

### 画面

8. 練習画面と同じ部品(TurnBadge/Board/MessageBar)を使う。自分の手番では従来どおり「あなたは　くろ　です」、CPU の手番では手番表示が「あいての　ばん」等と分かるようにする。
9. 全文言ひらがな・分かち書き(「OK」のみ例外。石数の数字は許容)。

### テスト

10. gameMachine のユニットテスト: 自分の着手→CPU 応手の遷移、プレイヤーパス、CPU パス、連続パスによる終局、盤面満杯による終局、勝ち/負け/引き分け判定、ミス累積、乱数注入した CPU の着手が合法手であること。
11. 既存テストを壊さない。

## やらないこと(スコープ外)

- がんばりの記録の保存・表示(T010)
- 色選択(しろでプレイ)・CPU 強さ調整
- 効果音
- `tasks/` `CLAUDE.md` `AGENTS.md` `scripts/` の変更

## 受け入れ基準(検証コマンド)

- [ ] `cd app && npx vitest run` が全件パス(gameMachine の全分岐テストを含む)
- [ ] `cd app && npm run build` が成功する
- [ ] 変更がコミット・push され、デプロイ成功後、公開URLで1局を最後までプレイでき、終局時に石数と勝敗メッセージ・ミス内訳が表示される(操作記録を作業ログに残す。終局まで長い場合、開発者ツールからの盤面状態の確認で終盤付近の検証を補ってよいが、パス発生と終局表示は実際に確認すること)
- [ ] 表示文言にカタカナ・漢字が含まれない(「OK」のみ例外。石数・ミス回数の数字は可)
- [ ] タスク完了時点で、当該タスク由来の差分・未追跡ファイルが `git status --short` に残っていないこと(Codex はコミット不可のため、コミット対象一覧の報告で代替)

## フィードバック(やり直し時にオーケストレーターが記入)

(なし)

## 作業ログ(担当エージェントが追記)

- 2026-07-15 Codex: モードCの対局状態機械、合法手から一様ランダムに選ぶCPU、対局画面、ホーム/画面切替を実装。プレイヤー操作は `practiceMachine` の `tapCell` / `pressOk` を再利用し、文言とミス加算を共通化。終局結果は `GameScreen` の `onComplete` コールバックで通知する形とした。
- テスト: `cd app && npx vitest run` は Vite の `net use` および Vitest fork がサンドボックスの `spawn EPERM` で起動前停止。Windowsドライブ確認のみ一時プリロードで無効化し、forkを使わない同等コマンド `npx vitest run --pool=threads` を実行して 11ファイル・108テスト全件成功（新規テストはプレイヤー着手→CPU応手、双方のパス、双方合法手なし/満杯終局、3勝敗、ミス累積、CPU合法手、開始局面からの1局完走を確認）。一時ファイルは削除済み。
- ビルド: 同じ一時プリロード下で `cd app && npm run build` を実行し成功（TypeScriptコンパイル、Vite本番ビルド、31モジュール変換）。一時ファイルは削除済み。
- 公開URL操作確認: このワーカー環境は `.git` 書き込み・commit・pushが禁止されているため未実施。オーケストレーターによるcommit/push・デプロイ後に、実際のパス表示と終局表示（石数・勝敗・ミス内訳）を確認する必要あり。
- コミットハッシュ: なし（Codex環境ではコミット不可）。
- 2026-07-15 verifier(Claude): T009 受け入れ検証を実施。判定=合格。対象コミット 93c2f50（origin/main。検証開始後に tasks/ のみを変更する d93364a が追加 push されたため fast-forward で追随。app配下の差分はなし）。
  - テスト: `cd app && npx vitest run` を実行し 11ファイル・108テスト全件成功（このワーカー環境では EPERM 制約は再現せず通常起動）。`gameMachine.test.ts` を通読し、要件10の全分岐（自分の着手→CPU応手の自動遷移、プレイヤーパス、CPUパス、両者連続パスによる終局、盤面満杯による終局、勝ち/負け/引き分け3ケース、ミス3種の累積、乱数注入したCPU着手の合法性検証）が実在すること、期待値が既知の盤面文字列やルールから独立に導出されており実装出力のコピーでないことを確認（例: `ends after both colors have no move` テストは `-`+`b`.repeat(63) から `discs:{b:63,w:0}` を直接算出、実装呼び出し結果をそのまま転記していない）。
  - ビルド: `cd app && npm run build` 成功（tsc + vite build、31モジュール）。
  - コードレビュー: `gameMachine.ts` は自分の手番の判定・ミス加算を独自実装せず `practiceMachine.ts` の `createPracticeState`/`tapCell`/`pressOk` をそのまま呼び出しており、置く・ひっくり返す・はやおしの文言/カウントの重複定義なし（`playerPractice`/`tapGameCell`/`pressGameOk` 参照）。`GameScreen.tsx` の `onComplete` 通知は `useEffect` で `state.phase === 'gameOver'` の場合のみ発火し、`おわる`(`onHome`)は state を一切変更せず直接ホームへ戻るため、途中終了時に通知されないことをコード上で確認済み。
  - 公開URL 1局とおしプレイ（最重要項目、初回実施）: https://giwarb.github.io/othello-beginner/ を開き（GitHub Actions run 29384500882→d93364a時点のrunとも success 確認後）、「さいごまで　うとう」を選択。
    - 自分の手番: 非合法マス(0番目)をタップし「そこには　おけないよ！」を確認（1回）。その後は盤面をDOM(ブラウザJS)から読み取り、標準オセロの合法手判定ロジックを自作して照合しながら実手を進行。序盤2手は手動操作で確認: 19番目に着手→「ひっくりかえそう」表示→対象コマを手動タップ→OKで確定→CPUが自動で20番目に着手し裏返り(28番目 b→w)が実際に適用されることをDOM状態(disc-shape)で確認。3手目では意図せず「はやおし」を誘発（ひっくり返す2箇所のうち1箇所のみ反転させてOKを押下）し「まだ　ひっくりかえる　こまが　あるよ」を確認、残りを反転させ再度OKで確定（ミス3種のうち2種を実地で誘発・確認）。
    - 以降はブラウザJS経由で自作の合法手判定・反転計算に従って自動クリック(pointerdown/mousedown/pointerup/mouseup/click の合成イベント)を連投し、終局まで最短で進行（要件どおり中盤以降は手加減せず）。CPUの応手は全ターンで自作の合法手判定と一致し続け、盤面不整合は一度も発生しなかった（実装のlegalMoves/applyMoveの正しさを間接的にクロスチェック）。
    - パス: 終盤で自分に合法手がない状態が自然発生し「おける　ばしょが　ないよ！　ぱす！」を実地確認（OK押下でCPU手番へ進行）。CPUパスの文言(「あいての　ばん」「あいては　ぱす！」)は自動連投の速度上リアルタイムでの目視は取れなかったが、コードとテストで担保済み、かつ本プレイでCPU側の着手はすべて正しく自動適用されていることをDOMで確認。
    - 終局: 盤面64マス到達前に双方合法手なしで終局。表示された内容: 「くろ　5こ　しろ　59こ」「あなたの　まけ」「おいた　ばしょの　まちがい 1かい」「ひっくりかえしの　まちがい 0かい」「はやおしの　かず 1かい」。ミス内訳の数値は上記の手動誘発（おくミス1回・はやおしミス1回、ひっくりかえしミスは誘発せず0回）と完全一致し、1局を通した累積が正しく動作していることを確認。
    - 「もういちど」押下で盤面が初期配置(27=w,28=b,35=b,36=w)にリセットされ新しい1局が開始することをDOM状態で確認。続けて「おわる」押下でホーム画面（「おせろの　れんしゅう」等のボタン一覧）に戻ることを確認。
  - 文言チェック: 本タスクで確認した全メッセージ・ラベル（そこには　おけないよ！/ひっくりかえそう/まだ　ひっくりかえる　こまが　あるよ/あいての　ばん/おける　ばしょが　ないよ！　ぱす！/あいては　ぱす！/あなたの　かち！・まけ・ひきわけ/くろ◯こしろ◯こ/ミス内訳3種/もういちど/おわる/OK）にカタカナ・漢字なし（「OK」のみ例外、数字は許容範囲）。
  - `git status --short`: verifier作業終了時点でクリーン（実装由来の残骸なし。origin/main の d93364a 追随後、ローカル差分なし）。
