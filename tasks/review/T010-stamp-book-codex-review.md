# T010 最終レビューレポート

## (a) 重大（done を止めるブロッカー）

なし。

## (b) 中（次タスクで対応すべき）

1. 記録加算が `useEffect` に依存しているため、結果表示直後の画面遷移で加算を取りこぼす可能性があります。

   [PracticeScreen.tsx](C:/Users/yoshi/work/othello-beginner/app/src/practice/PracticeScreen.tsx:37) と [GameScreen.tsx](C:/Users/yoshi/work/othello-beginner/app/src/game/GameScreen.tsx:29) は、結果状態を描画した後の passive effect で localStorage を更新しています。Preact の `useEffect` は描画後に遅延実行され、実行前にコンポーネントがアンマウントされると保留中の effect は実行されません。

   特に対局画面の「おわる」ボタンは終局前から表示されているため、CPU の最終着手とほぼ同時に押された場合など、`gameOver` には到達したものの完走・勝ちが記録されない競合が起こり得ます。要件の「終局まで到達したら完走 +1」を厳密には保証できません。

   記録処理を結果状態への遷移と同期させるか、少なくともアンマウント前に確実に実行される仕組みにすることを推奨します。

## (c) 軽微（記録のみ）

1. 記録 API のユニットテストは十分ですが、画面側の加算タイミングを検証するテストがありません。

   [records.test.ts](C:/Users/yoshi/work/othello-beginner/app/src/records/records.test.ts:1) は累積、壊れた JSON、読み書き例外を網羅しています。一方、練習結果・終局・途中終了・勝敗によって各 API が正しい回数だけ呼ばれることは未検証です。上記の遅延実行問題を含め、将来の回帰を検出しにくい状態です。

2. このレビュー環境では `npx vitest run` が Vite 設定読み込み時の子プロセス起動で `spawn EPERM` となり、テストを再実行できませんでした。読み取り専用のため、成果物を書き出す `npm run build` も再実行していません。作業ログ上では 116 テスト成功、ビルド・デプロイ成功が記録されています。

## (d) 総合判定

**合格**

指定範囲は1コミットで、変更対象は仕様に列挙されたアプリ内ファイルだけです。現在の `git status --short` は空で、`git diff --check` にも問題はありません。

localStorage の読み書きは例外保護され、壊れた項目は 0 扱いです。3種類の記録、50個上限表示、10列折り返し、ホームとの往復、リセット機能なし、ひらがな文言も仕様に沿っています。通常操作では練習・完走・勝ちの加算も正しく機能します。

結果表示直後の画面遷移による取りこぼしリスクはありますが、限定的なタイミング競合であり、通常フローと公開環境での受け入れ確認は成立しています。done を止めるブロッカーとは判断せず、次タスクでの堅牢化対象とします。